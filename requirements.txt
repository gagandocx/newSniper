# Core dependencies (install these when deploying)
requests>=2.31.0
beautifulsoup4>=4.12.0
jinja2>=3.1.0
markdown>=3.5.0
feedparser>=6.0.0
python-slugify>=8.0.0

# Note: The project includes stdlib-only fallbacks and can run
# without these dependencies in limited environments.
# For full functionality, install all packages above.
